---
interface Props {
  label: string;
  title: string;
  description: string;
}
const { label, title, description } = Astro.props;
---

<section class="page-intro wrap">
  <p class="section-note">{label}</p>
  <h1>{title}</h1>
  <p class="page-lead">{description}</p>
  <slot />
</section>
